
**Customers Module Enhancements (Continued):**
- **Search and Filtering:**
    - Added `searchTerm` state to `app/customers/page.tsx` for customer search.
    - Implemented search functionality in `fetchCustomers` to filter results based on `searchTerm`.
    - Added a search input field to the UI.
- **Pagination:**
    - Added `currentPage` and `itemsPerPage` states to `app/customers/page.tsx` for pagination.
    - Modified `fetchCustomers` to include `page` and `limit` query parameters.
    - Added "Previous" and "Next" pagination buttons to the UI.
- **Input Validation:**
    - Implemented `validateCustomer` function in `app/customers/page.tsx` to validate customer input (name, email, phone).
    - Integrated `validateCustomer` into `handleAddCustomer` and `handleUpdateCustomer` to display error messages for invalid input.
- **UI/UX Enhancements (Loading State):**
    - Added `loading` state to `app/customers/page.tsx`.
    - Displayed a loading indicator while fetching customer data.
